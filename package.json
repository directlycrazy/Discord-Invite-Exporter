{
  "name": "discord-invite-exporter",
  "description": "Exports invites of every server the user is in to a json file.",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start" : "node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "directlycrazy",
  "license": "MIT",
  "dependencies": {
    "discord.js-selfbot": "^12.0.2"
  }
}
